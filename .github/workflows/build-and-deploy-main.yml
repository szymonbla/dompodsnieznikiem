name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install and Build 🔧
        run: |
          yarn install
          yarn build

      - name: FTP Deploy 🚀
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: 45.84.205.63
          username: u396691420.dompodsnieznikiem.pl
          password: o0oxIwdQyg7L
          local-dir: ./build/
